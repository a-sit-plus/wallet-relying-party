app:
  public-context: http://localhost:8080/
  wrp:
    certificates:
      wrpac:
        password: test-only-password
spring:
  cloud:
    config:
      enabled: false
  config:
    import: "optional:configserver:"
